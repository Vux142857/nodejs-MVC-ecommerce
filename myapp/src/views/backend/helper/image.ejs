<% 
    imageHelper = (folder, imageName, alt = "Image", className = "img-responsive img-avatar") => {

        if(imageName == null){ 
            imageName = 'no-img.jpg';
        }
        return `<img class="${className}" style="width: 10rem;" src="/${folder + imageName}" alt="${alt}" />`;
    }
%>



