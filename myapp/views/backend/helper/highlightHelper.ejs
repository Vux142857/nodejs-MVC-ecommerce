<% highlightHelper=(name, keyword)=> {
    if(keyword !== ""){
    const regex = new RegExp(keyword, "gi");
    name = name.replace(regex, (search) => {
    return `<span class="text-red">${search}</span>`
    });
    return name;
    }
    return name;
    } %>