
<% sidebar = () =>{ %>
    <% let data = [
  {
    class: 'nav-item',
    href: '/admin/',
    class_href: 'nav-link',
    icon: 'nav-icon fas fa-tachometer-alt',
    name: 'Dashboard',
  },
  {
    class: 'nav-item menu-open',
    href: '#',
    class_href: 'nav-link',
    icon: 'nav-icon fa-solid fa-layer-group',
    name: 'Items',
    class_childItems: 'nav nav-treeview',
    childItems: [
      {
        class: 'nav-item',
        href: '/admin/items/list',
        class_href: 'nav-link',
        icon: 'far fa-circle nav-icon',
        name: 'List',
      },
      {
        class: 'nav-item',
        href: '/admin/items/form',
        class_href: 'nav-link',
        icon: 'far fa-circle nav-icon',
        name: 'Add items',
      }
    ]
  }
]; %>
    <ul class="nav nav-pills nav-sidebar flex-column" data-widget="treeview" role="menu" data-accordion="false">
        <% data.forEach(item => { %>
          <li class="<%= item.class %>">
            <a href="<%= item.href %>" class="<%= item.class_href %>">
              <i class="<%= item.icon %>"></i>
              <p>
                <%= item.name %>
              </p>
              <% if (item.childItems && item.childItems.length > 0) { %>
                <i class="right fas fa-angle-left"></i>
              <% } %>
            </a>
            <% if (item.childItems && item.childItems.length > 0) { %>
              <ul class="<%= item.class_childItems %>">
                <% item.childItems.forEach(childItem => { %>
                  <li class="<%= childItem.class %>">
                    <a href="<%= childItem.href %>" class="<%= childItem.class_href %>">
                      <i class="<%= childItem.icon %>"></i>
                      <p>
                        <%= childItem.name %>
                      </p>
                    </a>
                  </li>
                <% }); %>
              </ul>
            <% } %>
          </li>
        <% }); %>
      </ul>
<% } %>

